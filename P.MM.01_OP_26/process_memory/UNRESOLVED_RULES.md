# Unresolved rules

## Base-model-dependent versions

Decision No. 68 expresses versions and namespaces of `R.006`, `R.007` and their
imports as `Y.Y.Y` / `X.X.X`. Resolving these placeholders requires the selected
base-model profile; no version was inferred from process version `1.1.0`.
Their current independent selections are `active_version: null`; runtime status
on access is `UNRESOLVED_STRUCTURE_VERSION`.

## Detailed Body metadata

All 555 requisite rows are represented: 491 are verified and 64 have only an
external classifier-membership dependency. No field interpretation remains
unresolved. Of 201 message-rule rows, 195 are verified, one refers to external
registry state, and five are classified `INTERNAL_NORMATIVE_CONFLICT` because
their printed identifiers are absent from their declared structure. There are
zero generic interpretation items. Exact rows and required actions are in
`INTERPRETATION_UNRESOLVED_REPORT.md`.

## External classifiers and XSD

Classifier membership remains external. Physical XSD files are absent and
optional for this project. Neither condition invalidates the normative catalog.
